<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="{% block meta_description %}Waloude Prévoyance Décès{% endblock %}">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>💛​</text></svg>">
        {% block stylesheets %}
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
        <link rel="stylesheet" href="{{asset('css/style.css')}}">
        {% endblock %}

        
        <nav id="nav">
            <div class="logo">
                <img src="{{asset('img/diplome1.png')}}" alt="diplome logo" width="40" style="margin:10px">
                <p>Acade<span class="yellow-a">mia</span></p>
            </div>
            <ul>
                <li><a href="{{path('app_home')}}">Accueil</a></li>
                <li><a href="{{path('app_formation')}}">FORMATIONS</a></li>
                <li><a href="{{path('app_session')}}">SESSIONS</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{path('app_stagiaire')}}">STAGIAIRES</a></li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                <li><a href="{{path('app_formateur')}}">FORMATEURS</a></li>
                {% endif %}
                <li><a href="{{path('app_categorie')}}">CATEGORIES</a></li>
                <li><a href="{{path('app_module')}}">MODULES</a></li>
                <li><a href="{{path('app_programme')}}">PROGRAMMES</a></li>

                {% if app.user %}
                <li><a href="{{path('app_user')}}"><span class="material-symbols-outlined">
                    account_circle
                    </span></a></li>
                    <li><a href="{{path('app_logout')}}">Se déconnecter</a></li>
                {% else %}   
                    <li><a href="{{path('app_register')}}">S'inscrire</a></li>
                    <li><a href="{{path('app_login')}}">Se connecter</a></li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <button><a href="{{path('app_admin')}}">Administration</a></button>
                {% endif %}
                {# <button id="theme-toggle" class="dark-mode">
                    <span class="material-symbols-outlined">dark_mode</span>
                    <span class="material-symbols-outlined" style="display: none;">light_mode</span>
                </button> #}
                <button onclick="toggleDarkMode()" class="dark-mode"><span>&#9788;</span>
                </button>
            </ul>
            <div id="icons"></div>
        </nav>
    </head>
    {% for message in app.flashes('message') %}
        {{ message }}
    {% endfor %}
    <body>
        

        {% block body %}
        {% endblock %}

        <div class="bg-img"></div>
        
    </body>
    {% block javascripts %}
    <script src="{{asset('js/script.js')}}" ></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    {% endblock %}
</html>
