{% extends 'base.html.twig' %}

{% block title %}Ajouter un module{% endblock %}
{% block meta_description %}Ajout module{% endblock %}

{% block body %}
    <div class="container">
        {% if edit %}
            <h1>Editer un module</h1>
        {% else %}
            <h1>Ajouter un module</h1>
        {% endif %}

        {{ form_start(formAddModule) }}
    
            {{ form_row(formAddModule.nomModule)}}   
            {{ form_row(formAddModule.categorie)}}   
            {{ form_row(formAddModule.valider)}}
    
        {{ form_end(formAddModule) }}
    </div>

{% endblock %}