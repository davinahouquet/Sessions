{% extends 'base.html.twig' %}

{% block title %}Ajouter une formation{% endblock %}
{% block meta_description %}Ajout formation{% endblock %}

{% block body %}
    <div class="container">

        {% if edit %}
            <h1>Editer une formation</h1>
        {% else %}
            <h1>Ajouter une formation</h1>
        {% endif %}

        {{ form_start(formAddFormation) }}
    
            {{ form_row(formAddFormation.nomFormation)}}   
            {{ form_row(formAddFormation.valider)}}
    
        {{ form_end(formAddFormation) }}
    </div>

{% endblock %}